Screenshots for the walkthrough in index.html.

Drop PNGs here using the exact names below; the page swaps them in automatically
(a labelled placeholder shows until then, so nothing ever looks broken):

The six numbered steps:

  1-course-admin.png      Course Admin link in the course navbar
  2-import-export.png     Import / Export / Copy Components
  3-common-cartridge.png  The export options, "Common Cartridge" selected
  4-select-all.png        The "Select All Components" tick box
  5-confirm-export.png    "Confirm Components to Export" summary + Export button
  6-download-zip.png      "The course export was successful" + the download link

Inside the "If something looks different" panel (the recovery route, for people
who clicked Done or navigated away before downloading):

  bell-notification.png    The navbar bell with its orange dot
  bell-export-finished.png The bell list showing "Export finished"

Crop tight to the control in question. Keep each under ~200 KB.

Use a sandbox course, not a live one. These are published to a public site, so
check each image for learner names, enrolment counts and unpublished titles
before committing.
